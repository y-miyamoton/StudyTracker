<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.github.y_miyamoton.studytracker.repository.AppAuditRepository">

    <insert id="insert">
        INSERT INTO app_audit (user_id, action, entity, entity_id, detail, created_at)
        VALUES (#{userId}, #{action}, #{entity}, #{entityId}, #{detail}, CURRENT_TIMESTAMP)
    </insert>

    <select id="recent"
            resultType="com.github.y_miyamoton.studytracker.entity.AppAuditEntity">
        SELECT * FROM app_audit
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
        LIMIT #{limit}
    </select>

</mapper>